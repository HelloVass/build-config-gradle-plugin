package info.hellovass.sample

import info.hellovass.buildconfig_gradle_plugin.BuildConfig

object Constant {

    const val NAME = BuildConfig.NAME

    const val GROUP = BuildConfig.GROUP

    const val VERSION = BuildConfig.VERSION
}